package com.example.jwt;

// import com.example.jwt.security.JwtUtil;
// import io.jsonwebtoken.Claims;
// import org.junit.jupiter.api.Test;
// import org.springframework.beans.factory.annotation.Autowired;
// import org.springframework.boot.test.context.SpringBootTest;

// @SpringBootTest
class JwtApplicationTests {

    // private final JwtUtil jwtUtil;

    // @Autowired
    // public JwtApplicationTests(JwtUtil jwtUtil) {
    //     this.jwtUtil = jwtUtil;
    // }

    // @Test
    // void testGenerateToken() {
    //     // Generate a token for a test user
    //     String token = jwtUtil.generateToken("testUser");

    //     // Print the generated token to the console (for debugging purposes)
    //     System.out.println("Generated JWT Token: " + token);

    //     // Optionally, extract claims from the generated token and print them
    //     // Claims claims = jwtUtil.extractClaims(token);
    //     String claims = jwtUtil.extractClaims(token);
    //     System.out.println("Extracted Claims: " + claims);

    //     // You can print other debug info, such as the token's expiration time
    //     // System.out.println("Token Expiration: " + claims.getExpiration());
    // }

    // Optional: Add more tests to check other functionality of JwtUtil if needed
}
